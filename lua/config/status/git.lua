local util = require("util")
local M = {}
M.get_status = function()
	-- TODO:
	-- - commit history (expandable to see git diff for file, NTH: side-by-side diff)
	-- - last commit message and author
	-- - git status (not modified, modified, staged)
	return {}
end
return M
